# Mac Mirror Inventory Audit — turbomole-cosmo-v2

- **Date:** 2026-05-19
- **Host:** Mac (`/Users/youcef/Downloads/turbomole-cosmo-v2`)
- **Branch / HEAD:** `main` @ `0f60a2a`
- **Remote:** `origin` → `https://github.com/LEGRHIB/turbomole-cosmo.git`
- **Scope:** Read-only inventory of the local Mac working copy. Cluster (Wice) and remote (GitHub) state are referenced but not directly inspected.
- **Verdict:** **pass-with-warnings** — repository is structurally healthy, but there are tracked `__pycache__` artifacts, an oversized vendored PDF, and `.DS_Store` clutter that warrant cleanup.

---

## 1. Top-level inventory

| Path | Type | File count | Size | Description |
|---|---|---:|---:|---|
| `scripts/` | dir | 22 | 244 K | Shell + Python automation for the prep / submit / postprocess pipeline |
| `protocols/` | dir | 21 | 88 K | Per-protocol TURBOMOLE/COSMOtherm template directories (define.in, cosmoprep.in, slurm.tmpl) |
| `molecules/` | dir | 22 | 1.0 M | Per-molecule inputs: PDB/SDF sources, active `.xyz`, charge.txt |
| `docs/` | dir | 1 | 6.2 M | Vendored COSMOtherm Manual PDF (only file) |
| `.claude/` | dir | 4 | 40 K | Claude agent config + audit-reports archive |
| `.git/` | dir | 351 | 1.7 M | Git metadata |
| `README.md` | file | 1 | 16 K | Project entry-point doc |
| `config.sh` | file | 1 | 4 K | Shell config sourced by pipeline scripts (paths, SLURM defaults) |
| `.gitignore` | file | 1 | 4 K | Ignore rules for compute outputs and OS junk |
| `.DS_Store` | file | 1 | 8 K | macOS Finder metadata (untracked, ignored by `.gitignore`) |

---

## 2. Git state

### `git status --short`

```
 M protocols/cosmotherm-screen/screen-mixture.tmpl
 M scripts/cosmotherm_postprocess.py
?? .claude/
?? docs/
```

### `git log --oneline -n 10`

```
0f60a2a fix(propka_prep): resolve input PDB to absolute path before subprocess
69beb8f feat: add propka_prep.py for PROPKA-corrected protonation
57691c8 fix(xtb_preopt): namespace SLURM outputs by GFN level + job ID
6943432 xtb_preopt: add --solvent and --threads (Schmitz et al. 2020 recipe)
dcb07eb xtb_preopt: fix PATH order, add --sp diagnostic mode
4daa537 fix(xtb): lift stack limit (ulimit -s unlimited) in SLURM heredoc
6c955b5 feat: add BP-SVP-FINE-ANNEAL protocol with annealed Fermi schedule
561a3dd fix(cosmotherm_postprocess): detect NI- prefix on pr_ni columns
e976ab5 feat(cosmotherm): add pr_ni, clean mixture xlsx, unified screening ranking
fcd4a6c feat: add BP-SVP-FINE protocol (BP86/def2-SVP + RI-J + Fermi)
```

### `git remote -v`

```
origin  https://github.com/LEGRHIB/turbomole-cosmo.git (fetch)
origin  https://github.com/LEGRHIB/turbomole-cosmo.git (push)
```

Branches: `main` (local + remote), `remotes/origin/copilot/explore-codebase-structure`.

### Uncommitted edits (modified tracked files)

| Path | Diff stat (lines) |
|---|---:|
| `protocols/cosmotherm-screen/screen-mixture.tmpl` | 12 |
| `scripts/cosmotherm_postprocess.py` | 100 |

(Totals: 2 files, 86 insertions, 26 deletions per `git diff --stat`.)

### Untracked paths (full path from repo root)

- `.claude/` — entire directory
  - `.claude/settings.local.json`
  - `.claude/agents/repo-auditor.md`
  - `.claude/audit-reports/.gitkeep`
  - `.claude/audit-reports/audit-2026-05-11.md`
- `docs/` — entire directory
  - `docs/COSMOtherm_Manual.pdf` (6.2 M)

Notes:
- `.DS_Store` files exist locally (`./.DS_Store`, `molecules/.DS_Store`, `molecules/bradykinin/.DS_Store`, `protocols/.DS_Store`) but are already covered by `.gitignore` and do not appear in `git status`.
- `scripts/__pycache__/*` is **tracked** historically (see Section 3) — not in untracked list because at least one `.pyc` is committed.

---

## 3. `.gitignore` audit

### Current contents

```
# turbomole-cosmo .gitignore
# Layout: molecules/<molecule>/<protocol>/<generated-files>
# All TURBOMOLE / SLURM / xtb / COSMOtherm outputs are gitignored.

# === Generated TURBOMOLE files (in any molecule's protocol subdir) ===
molecules/**/coord
molecules/**/control
molecules/**/basis
molecules/**/auxbasis
molecules/**/mos
molecules/**/mos.bak
molecules/**/alpha
molecules/**/beta
molecules/**/energy
molecules/**/statistics
molecules/**/gradient
molecules/**/GEO_OPT_CONVERGED
molecules/**/job.last
molecules/**/job.start
molecules/**/logs/

# === COSMO / CCF / job output / errors ===
molecules/**/*.cosmo
molecules/**/*.ccf
molecules/**/*.out
molecules/**/*.err
molecules/**/*.stamp

# === SLURM job scripts (regenerated by submit_cosmo.sh / prep_cosmo.sh) ===
molecules/**/*.slurm

# === Per-molecule outputs / artifacts ===
molecules/**/cosmotherm/
molecules/**/GFN-xTB-OPT/

# === Geometry inputs and backups (regenerated by prepare_molecule.py + xtb_preopt) ===
molecules/**/*.xyz
molecules/**/*_pre_xtb.xyz
molecules/**/*.bak

# === Keep reference/ tracked (regression-test ground truth) ===
!molecules/*/reference/
!molecules/*/*/reference/

# === Python ===
__pycache__/
*.pyc
*.pyo

# === OS / editor junk ===
.DS_Store
Thumbs.db
*~
*.swp

# === Audit reports (local-only; agent writes them per-run) ===
.claude/audit-reports/
```

### Findings & recommendations

| Path | Status | Recommendation |
|---|---|---|
| `scripts/__pycache__/` (dir + 6 `.pyc` files) | **tracked** (committed before ignore rule, ignore rule now masks new files but old ones persist) | **delete + `git rm -r`**; `__pycache__/` is already in `.gitignore`. Current tracked `.pyc` files: `clean_pdb.cpython-313.pyc`, `clean_pdb.cpython-314.pyc`, `cosmotherm_postprocess.cpython-310.pyc`, `cosmotherm_postprocess.cpython-314.pyc`, `prepare_molecule.cpython-314.pyc`, `pdb2xyz.cpython-314.pyc`. (Only one — `clean_pdb.cpython-313.pyc` — shows up in `git ls-files`; verify and purge the rest if also tracked.) |
| `.claude/` (entire directory) | untracked | **partial gitignore**: `.claude/audit-reports/` is already ignored. Consider broadening to `.claude/` so `settings.local.json` and `agents/` are not accidentally committed (they are workstation- and Claude-CLI-specific). Or: commit `.claude/agents/repo-auditor.md` deliberately and ignore everything else. |
| `docs/` | untracked (contains only `COSMOtherm_Manual.pdf`, 6.2 M) | **decide**: either commit the manual deliberately (large blob warning — see Section 6) or gitignore `docs/*.pdf` and host the manual elsewhere. Right now it's a 6 M dangling blob waiting to be added. |
| `.DS_Store` (all locations) | untracked, ignored | OK — already covered by `.gitignore`. Safe to delete locally, will not affect git. |
| `protocols/.DS_Store`, `molecules/.DS_Store`, `molecules/bradykinin/.DS_Store` | untracked, ignored | Same as above. |
| `.vscode/`, `.idea/`, virtualenvs | not present | No action. |

---

## 4. Per-molecule subdir state

Expected pattern per molecule: source `*.pdb` or `*.sdf`, `{mol}.xyz`, `charge.txt`. PROPKA pipeline (new) may also leave `*_pH{N}.pdb` and `*_clean.pdb` intermediates and a `prep.log`.

### bombesin (SDF-source workflow)

| File | Size | Mtime |
|---|---:|---|
| `bombesin.sdf` | 27 087 | 2026-04-27 08:46 |
| `bombesin.xyz` | 10 989 | 2026-04-27 08:47 |
| `charge.txt` | 2 | 2026-04-27 08:47 |
| `prep.log` | 1 039 | 2026-04-27 08:47 |

**Status:** complete. SDF source + xyz + charge + prep log present. No propka/ (expected — SDF path does not invoke PROPKA).

### bradykinin (effectively empty)

| File | Size | Mtime |
|---|---:|---|
| `.DS_Store` | 6 148 | 2026-04-25 10:53 |
| `reference/` | (empty dir) | 2026-04-17 14:11 |

**Status:** **stale / unclear**. No PDB or SDF source, no `.xyz`, no `charge.txt`, no `prep.log`. Only a `.DS_Store` and an empty `reference/` directory. Flag as either:
- a placeholder for a future molecule (delete `.DS_Store` and leave `reference/.gitkeep`); or
- a leftover from an abandoned attempt (delete the whole directory).
Not in the user's "4 active" list — treat as candidate for removal once confirmed.

### lysozyme (PDB-source + pH 4.5 protonation)

| File | Size | Mtime |
|---|---:|---|
| `2LYZ.pdb` | 133 569 | 2026-04-23 11:00 |
| `lysozyme_clean.pdb` | 120 127 | 2026-04-23 11:00 |
| `lysozyme_pH4.5.pdb` | 334 160 | 2026-04-23 11:00 |
| `lysozyme.xyz` | 99 203 | 2026-04-23 11:00 |
| `charge.txt` | 2 | 2026-04-23 11:19 |
| `prep.log` | 1 622 | 2026-04-23 11:00 |

**Status:** complete. Sourced from 2LYZ. Has both `*_clean.pdb` (from clean_pdb.py) and `*_pH4.5.pdb` (protonated). No `propka/` subdir locally — likely because the user said the PROPKA-corrected regeneration at pH 4.6 was done on Wice (Task #11) and the corrected pH 4.6 intermediates have not been pulled back to the Mac mirror yet. The local `lysozyme_pH4.5.pdb` is the *pre-PROPKA* artifact and may now be stale relative to the cluster. Flag.

### vancomycin (PDB-source + pH 2.6 protonation)

| File | Size | Mtime |
|---|---:|---|
| `1SHO.pdb` | 97 848 | 2026-04-21 09:22 |
| `vancomycin_clean.pdb` | 39 613 | 2026-04-23 08:22 |
| `vancomycin_pH2.6.pdb` | 51 081 | 2026-04-23 08:22 |
| `vancomycin.xyz` | 9 133 | 2026-04-23 08:22 |
| `charge.txt` | 2 | 2026-04-23 08:22 |
| `prep.log` | 1 633 | 2026-04-23 08:22 |

**Status:** complete. Sourced from 1SHO, protonated at pH 2.6.

### vancomycin_2d (SDF-source workflow)

| File | Size | Mtime |
|---|---:|---|
| `vancomycin_2d.sdf` | 22 555 | 2026-04-27 08:23 |
| `vancomycin_2d.xyz` | 8 732 | 2026-05-07 11:52 |
| `charge.txt` | 2 | 2026-05-07 11:52 |
| `prep.log` | 1 144 | 2026-05-07 11:52 |

**Status:** complete. xyz/charge/prep.log were regenerated 2026-05-07 (~10 days after the SDF), suggesting `prepare_molecule.py --gen3d` was re-run. Consistent.

### Cross-molecule consistency

| Molecule | source format | clean.pdb | protonated.pdb | xyz | charge | prep.log | propka dir |
|---|---|:---:|:---:|:---:|:---:|:---:|:---:|
| bombesin | SDF | n/a | n/a | yes | yes | yes | no |
| bradykinin | — | no | no | no | no | no | no |
| lysozyme | PDB | yes | pH 4.5 | yes | yes | yes | no |
| vancomycin | PDB | yes | pH 2.6 | yes | yes | yes | no |
| vancomycin_2d | SDF | n/a | n/a | yes | yes | yes | no |

No `propka/` subdir exists for any molecule locally. If the cluster has generated `propka/` artifacts under lysozyme (after Task #11), they have not been mirrored back.

---

## 5. `scripts/` + `protocols/` inventory

### scripts/

| File | Size | Mode | One-line summary |
|---|---:|---|---|
| `_tune_scf.sh` | 3 511 | `0700` exec | Apply protocol-specific SCF tuning (scfiterlimit/scfdamp/scforbitalshift + Fermi smearing) to control file in cwd. |
| `batch_cosmo.sh` | 1 588 | `0700` exec | Run prep + submit for all molecules that have `.xyz` but no `.cosmo`. |
| `clean_pdb.py` | 4 784 | `0600` non-exec | Clean a PDB file: keep one copy of the molecule, drop waters/ions. |
| `cosmotherm_postprocess.py` | 37 775 | `0600` non-exec | Aggregate COSMOtherm solubility-screen results into clean tables matching the GUI's Solubility Solvent Screening layout. |
| `cosmotherm_screen.sh` | 6 707 | `0600` non-exec | Run COSMO-RS pure-solvent screen + binary mixtures for one molecule's `.cosmo` from a given DFT protocol. |
| `cosmotherm_setup.sh` | 1 848 | `0600` non-exec | Source COSMOtherm env and validate (idempotent; sourced by other scripts). |
| `diff_against_known_good.sh` | 2 321 | `0700` exec | Regression-test prep output against `molecules/<mol>/<protocol>/reference/{coord,control,basis,mos}`. |
| `full_pipeline.sh` | 9 413 | `0700` exec | End-to-end: PDB -> clean -> protonate -> XYZ -> TURBOMOLE -> submit. |
| `pdb2xyz.py` | 1 621 | `0600` non-exec | Convert a PDB file to XYZ. No external deps. |
| `prep_cosmo.sh` | 12 987 | `0700` exec | Run x2t + define + cosmoprep for one molecule / one protocol (login node or `--slurm`). |
| `prepare_molecule.py` | 18 104 | `0700` exec | Universal molecule -> XYZ prep for TURBOMOLE COSMO; PDB or SDF (--gen3d) input. Requires obabel. |
| `propka_prep.py` | 13 388 | `0600` non-exec | PROPKA-corrected protonation pipeline; runs pdb2pqr30 + PROPKA at user pH, writes XYZ + integer-charge `charge.txt`. |
| `run_cosmo.sh` | 2 003 | `0700` exec | End-to-end: prep + submit in one command. |
| `submit_cosmo.sh` | 3 730 | `0700` exec | Render SLURM template and submit the SCF job from `molecules/<mol>/<protocol>/`. |
| `verify_cosmo.sh` | 2 825 | `0700` exec | Post-job sanity checks on COSMO output. |
| `xtb_preopt.sh` | 16 282 | `0700` exec | GFN2-xTB geometry pre-optimization for rough starting geometries before DFT. |
| `__pycache__/` | (dir) | — | **Compiled bytecode cache — should not be in the repo** (see Section 3). |

**Executable-bit inconsistency:** several Python entry-points (`clean_pdb.py`, `cosmotherm_postprocess.py`, `pdb2xyz.py`, `propka_prep.py`) are `0600` (non-exec) despite carrying `#!/usr/bin/env python3` shebangs. Whether this is a real issue depends on whether the pipeline always invokes them as `python3 scripts/foo.py` (works) vs. `scripts/foo.py` (would fail). Same for the two shells in `0600`: `cosmotherm_screen.sh`, `cosmotherm_setup.sh` (the latter is `source`d so it's fine). Flag — see Section 7.

### protocols/

Each DFT protocol is expected to ship: `define.in`, `cosmoprep.in`, `slurm.tmpl`.

| Protocol | define.in | cosmoprep.in | slurm.tmpl | Missing |
|---|:---:|:---:|:---:|---|
| `BP-SVP-FINE` | yes (98 B) | yes (53 B) | yes (1 590 B) | — |
| `BP-SVP-FINE-ANNEAL` | yes (98 B) | yes (53 B) | yes (1 616 B) | — |
| `BP-TZVPD-FINE` | yes (100 B) | yes (53 B) | yes (1 483 B) | — |
| `BP-TZVPD-OPT` | yes (100 B) | yes (53 B) | yes (2 143 B) | — |
| `def2-SVP` | yes (58 B) | yes (53 B) | yes (901 B) | — |

All five DFT protocols are structurally complete.

The COSMOtherm protocol directory has a different layout:

| `protocols/cosmotherm-screen/` | Size | Notes |
|---|---:|---|
| `README.md` | 2 005 | Documents the COSMOtherm screening flow |
| `mixtures-binary.list` | 1 399 | Binary mixture solvent pairs for screening |
| `solvents-pure.list` | 859 | Pure-solvent list |
| `screen-mixture.tmpl` | 1 855 | COSMOtherm input template (mixtures) — **modified, uncommitted** |
| `screen-pure.tmpl` | 1 172 | COSMOtherm input template (pure) |

---

## 6. Large files (>5 MB)

| Path | Size | Tracked? | Note |
|---|---:|:---:|---|
| `docs/COSMOtherm_Manual.pdf` | 6.2 M | **No** (untracked) | Vendor reference manual. Large binary blob. **Do not commit**; either gitignore `docs/*.pdf` or move the manual outside the repo (e.g. cluster `$REPO_ROOT/../docs/` or shared OneDrive). Committing it would inflate every future clone. |

No other file in the working tree exceeds 5 MB. (The largest tracked file appears to be `molecules/lysozyme/lysozyme_pH4.5.pdb` at 326 KB.)

---

## 7. Anomalies & flags

1. **Bradykinin directory is effectively empty** — `molecules/bradykinin/` contains only a stray `.DS_Store` and an empty `reference/` dir. Not in the user's 4 active molecules. Decide: delete the directory, or scaffold it (drop a source PDB/SDF + run `prepare_molecule.py`).

2. **`scripts/__pycache__/` is partially tracked.** `git ls-files` reports `scripts/__pycache__/clean_pdb.cpython-313.pyc` as a tracked file even though `__pycache__/` is in `.gitignore`. Six other `.pyc` files exist locally and are presumably ignored. This is a stale checked-in artifact from before the ignore rule was added. **Action:** `git rm -r --cached scripts/__pycache__` and `rm -rf scripts/__pycache__`.

3. **Three uncommitted/untracked changes that look real, not accidental:**
   - `protocols/cosmotherm-screen/screen-mixture.tmpl` — 12 lines modified, paired with...
   - `scripts/cosmotherm_postprocess.py` — 100 lines modified.
   Together these look like an in-flight feature edit on the COSMOtherm postprocess + mixture template. Confirm intent — commit or revert.
   - `docs/COSMOtherm_Manual.pdf` — untracked 6 M PDF (Section 6).
   - `.claude/` directory — untracked agent config + a previous audit report (Section 3).

4. **No `propka/` subdir under `molecules/lysozyme/` despite the recent PROPKA work** (Tasks #9–#11). The pH 4.6 corrected intermediates apparently exist only on Wice and have not been pulled back to the Mac mirror. The locally present `lysozyme_pH4.5.pdb` (April 23) is now older than the canonical inputs. Consider rsyncing the propka artefacts back, or document that the Mac is read-only for these.

5. **Executable bit inconsistency in `scripts/`** — Python scripts and two shell scripts are `0600` rather than `0700` despite having shebangs. Not a runtime blocker (the pipeline invokes them with explicit `python3`/`bash`), but harmless to fix with `chmod +x`. Notable cases: `clean_pdb.py`, `cosmotherm_postprocess.py`, `pdb2xyz.py`, `propka_prep.py`, `cosmotherm_screen.sh`. (`cosmotherm_setup.sh` is intentionally non-exec because it is sourced.)

6. **`.DS_Store` files persist locally** at `./`, `molecules/`, `molecules/bradykinin/`, `protocols/`. They are correctly gitignored and inert, but could be swept (`find . -name .DS_Store -delete`) to reduce noise.

7. **`docs/` directory exists only to host the COSMOtherm manual**, which is untracked. The audit report you are now reading is being written to `docs/audit/`, which would be the *first* tracked content under `docs/` if committed. Decide whether `docs/audit/` should be tracked (recommended — these are useful project artefacts) and whether `docs/COSMOtherm_Manual.pdf` should be globally ignored.

8. **`.claude/audit-reports/audit-2026-05-11.md` exists** — there is already a prior audit-report archive from the `repo-auditor` agent. The current report is being written under `docs/audit/` instead of `.claude/audit-reports/`, by design (per the user's task spec). No conflict, just be aware that two audit-report locations now coexist.

9. **Two recent feature branches landed on `main`:** propka_prep (May 18–19) and BP-SVP-FINE-ANNEAL (May 16–17). No corresponding `molecules/*/` regressions or `protocols/*/reference/` updates were committed alongside — verify regression baselines under `molecules/*/reference/` are still authoritative.

10. **Remote branch `origin/copilot/explore-codebase-structure`** exists with no obvious local counterpart. Likely a GitHub-Copilot-bot exploration branch; safe to ignore but consider pruning on the remote.

---

## Quick-action checklist (suggested, not auto-applied)

- [ ] `git rm -r --cached scripts/__pycache__` and add a check to CI.
- [ ] Decide on `.claude/` — broaden gitignore or commit `agents/repo-auditor.md` only.
- [ ] Decide on `docs/COSMOtherm_Manual.pdf` — gitignore `docs/*.pdf` and host the manual outside the repo, OR commit deliberately with a LFS warning.
- [ ] Commit or revert the in-flight `cosmotherm_postprocess.py` + `screen-mixture.tmpl` edits.
- [ ] Remove or scaffold `molecules/bradykinin/`.
- [ ] `find . -name .DS_Store -delete` (idempotent housekeeping).
- [ ] Sync `propka/` artefacts back from Wice for lysozyme, or document the pH 4.5 vs pH 4.6 divergence.
- [ ] `chmod +x` the python entry-points in `scripts/` for consistency (optional).
- [ ] Prune `origin/copilot/explore-codebase-structure` if abandoned.
